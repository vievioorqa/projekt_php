{% include 'default/_form.html.twig' with {
    page_title: 'Edit account'|trans({'%id%': app.user.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'masterpiece_index',
}
%}